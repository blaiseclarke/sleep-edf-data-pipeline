services:
  sleep-pipeline:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: on-failure:3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
